{% extends 'base.html.twig' %}

{% block title %}Modifier l'automatisation-ligne{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">Modifier l'automatisation-ligne</h1>

        {{ include('automatisation_ligne/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="d-flex gap-2 mt-3">
            <a href="{{ path('app_automatisation_ligne_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>

        {{ include('automatisation_ligne/_delete_form.html.twig') }}
    </div>
{% endblock %}
