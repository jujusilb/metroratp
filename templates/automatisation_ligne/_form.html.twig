{{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_row(form.ligne) }}
    {{ form_row(form.automatisation) }}
    {{ form_row(form.dateDeMiseEnPlace) }}

    <button type="submit" class="btn btn-primary mt-2">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
