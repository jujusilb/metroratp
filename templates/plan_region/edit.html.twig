{% extends 'base.html.twig' %}

{% block title %}Modifier le plan régional{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">Modifier le plan régional</h1>

        {{ include('plan_region/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="d-flex gap-2 mt-3">
            <a href="{{ path('app_plan_region_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>

        {{ include('plan_region/_delete_form.html.twig') }}
    </div>
{% endblock %}
