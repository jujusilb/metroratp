{{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_row(form.nom) }}
    {{ form_row(form.description) }}
    {{ form_row(form.tache) }}
    {{ form_row(form.datetimeCreation) }}
    {{ form_row(form.datetimeAchevement) }}

    <button type="submit" class="btn btn-primary mt-2">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
