{% extends 'base.html.twig' %}

{% block title %}Modifier la fontaine à eau{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">Modifier la fontaine à eau</h1>

        {{ include('fontaine_eau/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="d-flex gap-2 mt-3">
            <a href="{{ path('app_fontaine_eau_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>

        {{ include('fontaine_eau/_delete_form.html.twig') }}
    </div>
{% endblock %}
