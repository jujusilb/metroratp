{% extends 'base.html.twig' %}

{% block title %}Modifier le type de desserte{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">Modifier le type de desserte</h1>

        {{ include('type_desserte/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="d-flex gap-2 mt-3">
            <a href="{{ path('app_type_desserte_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>

        {{ include('type_desserte/_delete_form.html.twig') }}
    </div>
{% endblock %}
