{% extends 'base.html.twig' %}

{% block title %}Créer nouvel accès{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">Créer nouvel accès</h1>

        {{ include('acces/_form.html.twig') }}

        <a href="{{ path('app_acces_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
    </div>
{% endblock %}
