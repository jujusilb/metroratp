{% extends 'base.html.twig' %}

{% block title %}Modifier l'accès{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">Modifier l'accès</h1>

        {{ include('acces/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="d-flex gap-2 mt-3">
            <a href="{{ path('app_acces_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>

        {{ include('acces/_delete_form.html.twig') }}
    </div>
{% endblock %}
