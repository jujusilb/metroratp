{{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_row(form.artId) }}
    {{ form_row(form.nom) }}
    {{ form_row(form.ville) }}
    {{ form_row(form.type) }}
    {{ form_row(form.zoneTarifaire) }}
    {{ form_row(form.estAccessible) }}
    {{ form_row(form.signalisationSonore) }}
    {{ form_row(form.signalisationVisuelle) }}
    {{ form_row(form.latitude) }}
    {{ form_row(form.longitude) }}
    {{ form_row(form.station) }}

    <button type="submit" class="btn btn-primary mt-2">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
