{% extends 'base.html.twig' %}

{% block title %}Modifier l'arrêt transporteur{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">Modifier l'arrêt transporteur</h1>

        {{ include('arret_transporteur/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="d-flex gap-2 mt-3">
            <a href="{{ path('app_arret_transporteur_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>

        {{ include('arret_transporteur/_delete_form.html.twig') }}
    </div>
{% endblock %}
