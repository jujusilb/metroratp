{% extends 'base.html.twig' %}

{% block title %}Modifier le point d'intérêt{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">Modifier le point d'intérêt</h1>

        {{ include('point_interet/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="d-flex gap-2 mt-3">
            <a href="{{ path('app_point_interet_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>

        {{ include('point_interet/_delete_form.html.twig') }}
    </div>
{% endblock %}
