{% extends 'base.html.twig' %}

{% block title %}Modifier le point de vente{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">Modifier le point de vente</h1>

        {{ include('point_de_vente/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="d-flex gap-2 mt-3">
            <a href="{{ path('app_point_de_vente_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>

        {{ include('point_de_vente/_delete_form.html.twig') }}
    </div>
{% endblock %}
