{% extends 'base.html.twig' %}

{% block title %}Modifier la desserte{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">Modifier la desserte</h1>

        {{ include('desserte/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="d-flex gap-2 mt-3">
            <a href="{{ path('app_desserte_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>

        {{ include('desserte/_delete_form.html.twig') }}
    </div>
{% endblock %}
