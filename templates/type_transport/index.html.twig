{% extends 'base.html.twig' %}

{% block title %}Types{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">Types</h1>

        {{ include('tools/filtre_alphabet.html.twig', {
            'route': 'app_type_transport_index',
            'lettreSelectionnee': lettre,
            'recherche': recherche,
            'libelleRecherche': 'un type',
        }) }}
        {{ include('tools/_onglets.html.twig', {onglets: [
            {label: 'Transport', route: 'app_type_transport_index', actif: true},
            {label: 'Matériel', route: 'app_type_materiel_index', actif: false},
            {label: 'Tronçon', route: 'app_type_troncon_index', actif: false},
        ]}) }}

        <table class="table align-middle">
            <thead>
                <tr>
                    <th>{{ knp_pagination_sortable(type_transports, 'Label', 't.label') }}</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for type_transport in type_transports %}
                <tr>
                    <td>{{ type_transport.label }}</td>
                    <td>
                        <a href="{{ path('app_type_transport_show', {'id': type_transport.id}) }}" class="btn btn-info btn-sm" title="Affichage" aria-label="Affichage"><i class="bi bi-eye"></i></a>
                        {% if is_granted('ROLE_ADMIN') %}
                            <a href="{{ path('app_type_transport_edit', {'id': type_transport.id}) }}" class="btn btn-warning btn-sm" title="Modifier" aria-label="Modifier"><i class="bi bi-wrench"></i></a>
                        {% endif %}
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="2">Aucun enregistrement trouvé</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        {{ knp_pagination_render(type_transports) }}

        {% if is_granted('ROLE_ADMIN') %}
            <a href="{{ path('app_type_transport_new') }}" class="btn btn-primary">Créer nouveau type de transport</a>
        {% endif %}
    </div>
{% endblock %}
