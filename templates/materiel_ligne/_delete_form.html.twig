<form method="post" action="{{ path('app_materiel_ligne_delete', {'id': materiel_ligne.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ materiel_ligne.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
