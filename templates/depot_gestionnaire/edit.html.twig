{% extends 'base.html.twig' %}

{% block title %}Modifier le dépôt-gestionnaire{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">Modifier le dépôt-gestionnaire</h1>

        {{ include('depot_gestionnaire/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="d-flex gap-2 mt-3">
            <a href="{{ path('app_depot_gestionnaire_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>

        {{ include('depot_gestionnaire/_delete_form.html.twig') }}
    </div>
{% endblock %}
