{% extends 'base.html.twig' %}

{% block title %}Modifier la raison{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">Modifier la raison</h1>

        {{ include('raison/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="d-flex gap-2 mt-3">
            <a href="{{ path('app_raison_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>

        {{ include('raison/_delete_form.html.twig') }}
    </div>
{% endblock %}
