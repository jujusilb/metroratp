<form method="post" action="{{ path('app_tache_delete', {'id': tache.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ? Ses étapes seront aussi supprimées.');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tache.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
