{{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_row(form.ligne) }}
    {{ form_row(form.type) }}
    {{ form_row(form.nom) }}
    {{ form_row(form.url) }}

    <button type="submit" class="btn btn-primary mt-2">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
