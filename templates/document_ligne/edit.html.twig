{% extends 'base.html.twig' %}

{% block title %}Modifier le document de ligne{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">Modifier le document de ligne</h1>

        {{ include('document_ligne/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="d-flex gap-2 mt-3">
            <a href="{{ path('app_document_ligne_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>

        {{ include('document_ligne/_delete_form.html.twig') }}
    </div>
{% endblock %}
