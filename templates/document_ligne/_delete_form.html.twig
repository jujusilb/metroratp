{% if is_granted('ROLE_ADMIN') %}
<form method="post" action="{{ path('app_document_ligne_delete', {'id': document.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ document.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
{% endif %}
