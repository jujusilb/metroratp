{% extends 'base.html.twig' %}

{% block title %}Modifier le projet d'arrêt{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">Modifier le projet d'arrêt</h1>

        {{ include('projet_arret/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="d-flex gap-2 mt-3">
            <a href="{{ path('app_projet_arret_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>

        {{ include('projet_arret/_delete_form.html.twig') }}
    </div>
{% endblock %}
