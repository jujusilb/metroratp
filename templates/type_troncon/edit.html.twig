{% extends 'base.html.twig' %}

{% block title %}Modifier le type de tronçon{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">Modifier le type de tronçon</h1>

        {{ include('type_troncon/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="d-flex gap-2 mt-3">
            <a href="{{ path('app_type_troncon_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>

        {{ include('type_troncon/_delete_form.html.twig') }}
    </div>
{% endblock %}
