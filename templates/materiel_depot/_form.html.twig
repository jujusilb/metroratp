{{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_row(form.materiel) }}
    {{ form_row(form.depot) }}
    {{ form_row(form.arrivee) }}
    {{ form_row(form.fin) }}
    {{ form_row(form.effectif) }}
    {{ form_row(form.effectifDate) }}

    <button type="submit" class="btn btn-primary mt-2">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
