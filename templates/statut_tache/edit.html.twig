{% extends 'base.html.twig' %}

{% block title %}Modifier le statut de tâche{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">Modifier le statut de tâche</h1>

        {{ include('statut_tache/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="d-flex gap-2 mt-3">
            <a href="{{ path('app_statut_tache_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>

        {{ include('statut_tache/_delete_form.html.twig') }}
    </div>
{% endblock %}
