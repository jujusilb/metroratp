{% extends 'base.html.twig' %}

{% block title %}Modifier le style d'accès{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">Modifier le style d'accès</h1>

        {{ include('style_acces/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="d-flex gap-2 mt-3">
            <a href="{{ path('app_style_acces_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>

        {{ include('style_acces/_delete_form.html.twig') }}
    </div>
{% endblock %}
