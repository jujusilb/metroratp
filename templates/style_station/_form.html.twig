{% import 'tools/_collection_datee.html.twig' as collections %}

{{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_row(form.label) }}
    {{ collections.collection_datee(form.dessertes, 'Dessertes (id)', 'style_station_dessertes') }}

    <button type="submit" class="btn btn-primary mt-2">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
