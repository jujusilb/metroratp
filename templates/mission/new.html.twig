{% extends 'base.html.twig' %}

{% block title %}Créer nouvelle mission{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">Créer nouvelle mission</h1>

        {{ include('mission/_form.html.twig') }}

        <a href="{{ path('app_mission_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
    </div>
{% endblock %}
