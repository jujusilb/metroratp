{{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_row(form.label) }}
    {{ form_row(form.plan) }}
    {{ form_row(form.poleEchange) }}
    {{ form_row(form.acces) }}
    {{ form_row(form.pointsInteret) }}

    <button type="submit" class="btn btn-primary mt-2">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
