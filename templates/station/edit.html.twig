{% extends 'base.html.twig' %}

{% block title %}Modifier la station{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">Modifier la station</h1>

        {{ include('station/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="d-flex gap-2 mt-3">
            <a href="{{ path('app_station_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>

        {{ include('station/_delete_form.html.twig') }}
    </div>
{% endblock %}
