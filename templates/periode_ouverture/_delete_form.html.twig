{% if is_granted('ROLE_ADMIN') %}
<form method="post" action="{{ path('app_periode_ouverture_delete', {'id': periode_ouverture.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ periode_ouverture.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
{% endif %}
