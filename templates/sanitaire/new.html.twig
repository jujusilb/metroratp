{% extends 'base.html.twig' %}

{% block title %}Créer un sanitaire{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">Créer un sanitaire</h1>

        {{ include('sanitaire/_form.html.twig') }}

        <a href="{{ path('app_sanitaire_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
    </div>
{% endblock %}
