{% extends 'base.html.twig' %}

{% block title %}Modifier le sanitaire{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">Modifier le sanitaire</h1>

        {{ include('sanitaire/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="d-flex gap-2 mt-3">
            <a href="{{ path('app_sanitaire_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>

        {{ include('sanitaire/_delete_form.html.twig') }}
    </div>
{% endblock %}
