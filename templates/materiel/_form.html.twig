{% import 'tools/_collection_datee.html.twig' as collections %}

{{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_row(form.label) }}
    {{ form_row(form.anneeProduction) }}
    {{ form_row(form.constructeur) }}
    {{ form_row(form.vitesseMaxKmh) }}
    {{ form_row(form.typeMateriel) }}

    {{ collections.collection_datee(form.materielLignes, 'Lignes desservies', 'materiel_materielLignes') }}

    <button type="submit" class="btn btn-primary mt-2">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
