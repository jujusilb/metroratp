{{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_row(form.ligne) }}
    {{ form_row(form.station) }}
    {{ form_row(form.destination) }}
    {{ form_row(form.acces) }}
    {{ form_row(form.labelPosition) }}
    {{ form_row(form.position) }}
    {{ form_row(form.positionMax) }}
    {{ form_row(form.equipement) }}

    <button type="submit" class="btn btn-primary mt-2">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
