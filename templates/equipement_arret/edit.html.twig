{% extends 'base.html.twig' %}

{% block title %}Modifier l'équipement d'arrêt{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">Modifier l'équipement d'arrêt</h1>

        {{ include('equipement_arret/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="d-flex gap-2 mt-3">
            <a href="{{ path('app_equipement_arret_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>

        {{ include('equipement_arret/_delete_form.html.twig') }}
    </div>
{% endblock %}
