{% extends 'base.html.twig' %}

{% block title %}Modifier la sortie{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">Modifier la sortie</h1>

        {{ include('sortie/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="d-flex gap-2 mt-3">
            <a href="{{ path('app_sortie_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>

        {{ include('sortie/_delete_form.html.twig') }}
    </div>
{% endblock %}
