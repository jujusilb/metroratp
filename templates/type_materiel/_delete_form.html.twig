<form method="post" action="{{ path('app_type_materiel_delete', {'id': type_materiel.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_materiel.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
